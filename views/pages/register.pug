extends ../templates/base

block content
    .container
        .row.justify-content-center.align-items-center(style='height:60vh')
            .col-6
                .card
                    h3.card-header.text-center Kayıt Ol
                    .card-body
                        form(action='/register' method="post" autocomplete='on')
                            .form-group
                                input.form-control(type='text' name='username' placeholder="username")
                            .form-group
                                input.form-control(type='text' name='name' placeholder="name")
                            .form-group
                                input.form-control(type='text' name='email' placeholder="email")
                            .form-group
                                input.form-control(type='password' name='password' placeholder="password")
                            .form-group
                                input.form-control(type='password' name='passwordconfirm' placeholder="passwordconfirm")
                            button#sendlogin.btn.btn-block.btn-primary(type='submit') Kaydol
